services:
  taskflow-pro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: msbrossai-taskflow-pro
    ports:
      - "8887:80"
    networks:
      - msbrossai-network
    restart: unless-stopped

networks:
  msbrossai-network:
    driver: bridge
